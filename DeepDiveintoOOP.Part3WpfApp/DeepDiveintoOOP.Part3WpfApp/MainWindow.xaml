<Window x:Class="DeepDiveintoOOP.Part3WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DeepDiveintoOOP.Part3WpfApp"
        mc:Ignorable="d"
        Title="Банковская система" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="ClientsGrid" AutoGenerateColumns="False" Margin="10,10,10,200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Счета" Binding="{Binding Accounts.Count}" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,10">
            <Button Content="Добавить клиента" Click="AddClient_Click" Margin="5" />
            <Button Content="Управление счетами" Click="ManageAccounts_Click" Margin="5" />
            <Button Content="Перевод средств" Click="TransferFunds_Click" Margin="5" />
        </StackPanel>
    </Grid>
</Window>
